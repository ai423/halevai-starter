# CLAUDE.md

הריפו הזה הוא כלי עסקי אחד, שנבנה בסדנת "הלוואי שהיה אפשר" של GR8MINDS.
מי שמבקש ממך דברים כאן הוא איש עסקים, לא מפתח. הכללים האלה גוברים על כל ברירת מחדל שלך.

## מה בונים

- הכלי מוגדר בקובץ `SPEC.md` (כרטיס האפיון). קרא אותו לפני כל עבודה.
- בונים רק את מה שכתוב תחת "פלט", עם הקלט שכתוב תחת "קלט".
- מה שכתוב תחת "הכלי לא עושה" הוא איסור מפורש, גם אם זה נראה לך מועיל.
- אם `SPEC.md` ריק או לא ברור, עצור ושאל. אל תמציא מוצר.

## איך עובדים

1. **דבר אחד בכל בקשה.** אם הבקשה כוללת יותר מדבר אחד, שאל מה קודם ובנה רק אותו.
2. **תוכנית לפני קוד.** לפני כל שינוי, הצג תוכנית של עד 5 שורות בעברית פשוטה (מה ישתנה, מה יראה המשתמש) וחכה לאישור.
3. **הבנייה חייבת לעבור.** לפני שאתה מסיים, הרץ `npm run build` וודא שהוא עובר בלי שגיאות. זה מה ש-Vercel מריץ.
4. **בלי תוספות.** בלי בסיס נתונים, בלי התחברות והרשאות, בלי ספריות חדשות, בלי עמודים נוספים, אלא אם התבקשו במפורש.
5. **נתונים.** עד שמבקשים אחרת, נתונים נשמרים ב-`localStorage` בדפדפן, או נקראים מקובץ שהמשתמש מעלה, או מקובץ בתיקיית `examples/`.
6. **סודות.** רק במשתני סביבה. לעולם לא בקוד, לעולם לא בגיט. ראה `.env.example`.
7. **AI בזמן ריצה** רק דרך `lib/claude.ts`, ורק אם הכרטיס דורש סיווג, סיכום או ניסוח. רוב הכלים לא צריכים.
8. **כשמשהו נשבר,** תסביר במשפט אחד בעברית מה קרה, ואז תתקן. בלי הרצאות.

## ממשק

- **לפני כל שינוי במסך, קרא את `docs/UX-UI.md`.** הוא הכללים המלאים. בסוף כל תוכנית, עבור על הצ'קליסט שבסופו ודווח מה מתקיים.
- הכל בעברית. `dir="rtl"` כבר מוגדר ב-`app/layout.tsx`, אל תשנה.
- מובייל תחילה. כל מסך חייב לעבוד ברוחב 400 פיקסלים בלי גלילה אופקית.
- משתמשים רק בטוקנים ובמחלקות מ-`app/globals.css` (`.container`, `.card`, `.btn`, `.field`, `.status`, `.table-wrap`, `.numeric`, `.empty`, `.skeleton`, `.toast`). אין hex בקומפוננטות. לא מוסיפים גופנים.
- כפתור ראשי אחד בכל מסך. תווית מעל כל שדה. מספרים ב-`.numeric`.
- כל מסך מטפל בארבעה מצבים: ריק, טוען, שגיאה, הצלחה.
- טקסט של כפתורים ומסרים: קצר, בגוף שני, אומר מה יקרה ("העלאת קובץ") ומה קרה ("נקראו 10 שורות").

## מבנה

- `app/page.tsx`: הדף הראשי. הכלי חי כאן. מחליפים את תוכן התבנית.
- `app/globals.css`: טוקנים ומחלקות.
- `tool.config.ts`: שם הבעלים ושם הכלי.
- `lib/claude.ts`: עטיפה ל-Claude API (שרת בלבד).
- `app/api/ask/route.ts`: דוגמה לנקודת קצה. למחוק אם לא צריך.
- `scripts/daily.mjs` + `.github/workflows/daily.yml`: הרצה יומית מתוזמנת. כבויה עד שמבקשים.
- `examples/`: קובצי נתונים לדוגמה בעברית.
- `docs/UX-UI.md`: כללי הממשק. קוראים לפני כל מסך.
- `docs/`: כרטיס האפיון, הפרומפטים, הכללים. לא לגעת.

## שפה

- כותב למשתמש בעברית, בגוף שני, קצר.
- שמות טכניים (Vercel, PR, build) נשארים באנגלית.
- בלי מקפים ארוכים בטקסטים. במקומם פסיק, נקודה או נקודתיים.
